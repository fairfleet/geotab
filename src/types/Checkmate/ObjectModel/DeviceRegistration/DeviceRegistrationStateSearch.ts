//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "../Search";
import { RegistrationStatus } from "../Registration/Gateway/RegistrationStatus";

/** The object used to specify the arguments when searching for a {@link DeviceRegistrationState}. */
export interface DeviceRegistrationStateSearch extends Search {
  /** Gets or sets the actual registration states to search for. */
  actualStates: RegistrationStatus[];
  /** Gets or sets the device ID's to search for. */
  deviceIds: string[];
  /** Gets or sets the hardware ID's to search for. */
  hardwareIds: number[];
  /** Gets or sets the intended registration states to search for. */
  intendedStates: RegistrationStatus[];
  /** Gets or sets a value indicating only current states are to be searched. */
  onlyCurrentState: boolean;
}
