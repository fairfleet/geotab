//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Entity } from "./Entity";
import { Device } from "./Device";
import { DataPrivacyType } from "./DataPrivacyType";

/** Indicates when a {@link DeviceDataPrivacyChange.DataPrivacyType} was changed. */
export interface DeviceDataPrivacyChange extends Entity {
  /** Gets or sets the type of the trip. */
  dataPrivacyType: DataPrivacyType;
  /** Gets or sets the date time. */
  dateTime: Date;
  /** Gets or sets the device. */
  device: Device;
  /** Gets a value indicating whether the privacy mode is private. */
  isPrivate: boolean;
  /** Gets a value indicating whether the privacy mode is public. */
  isPublic: boolean;
  /** Gets a value indicating whether the privacy mode is private strict. */
  isStrict: boolean;
}
