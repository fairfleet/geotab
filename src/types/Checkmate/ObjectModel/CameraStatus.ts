//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { EntityWithVersion } from "./EntityWithVersion";
import { Camera } from "./Camera";
import { CameraDeviceStatus } from "./CameraDeviceStatus";

/** Stores camera online/offline status information. */
export interface CameraStatus extends EntityWithVersion {
  /** Gets or sets the camera. */
  camera: Camera;
  /** Gets or sets the last updated timestamp. */
  lastUpdated: Date;
  /** Gets or sets the camera status. */
  status: CameraDeviceStatus;
}
