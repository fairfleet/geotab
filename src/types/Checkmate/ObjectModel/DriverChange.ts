//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { EntityWithVersion } from "./EntityWithVersion";
import { DriverChangeType } from "./DriverChangeType";
import { Device } from "./Device";
import { Driver } from "./Driver";

/**
 *  Information about timing of a {@link Driver} change.
 */
export interface DriverChange extends EntityWithVersion {
  /** Gets or sets the date and time of the driver change. */
  dateTime: Date;
  /** Gets or sets the {@link Device} that had the driver change. */
  device: Device;
  /** Gets or sets the {@link Driver} associated with the change. */
  driver: Driver;
  /** Gets or sets the {@link DriverChangeType}. */
  type: DriverChangeType;
}
