//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "./Search";
import { DeviceSearch } from "./DeviceSearch";
import { IntervalAggregateType } from "./IntervalAggregateType";

/** The object used to specify the arguments when searching for {@link VehicleDowntime}. */
export interface VehicleDowntimeSearch extends Search {
  /** Gets or sets the aggregate type to filter by. */
  aggregateType: IntervalAggregateType;
  /**
   * Gets or sets search for {@link VehicleDowntime}s matching this
   *  {@link DeviceSearch}.
   *  Providing a device ID will search for any VehicleDowntime that are assigned to that Device.
   *  Available DeviceSearch options are:
   *  <list><item><description>Id</description></item></list>
   */
  deviceSearch: DeviceSearch;
  /** Gets or sets search for {@link VehicleDowntime}s from this local date or after. */
  fromLocalDate: Date;
  /** Gets or sets the repair class to filter by. */
  repairClass: string;
  /** Gets or sets the source reason to filter by. */
  sourceReason: string;
  /** Gets or sets search for {@link VehicleDowntime}s to this local date or before. */
  toLocalDate: Date;
}
