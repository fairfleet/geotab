//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { EntityWithVersion } from "./EntityWithVersion";
import { DtcSeverity } from "./Engine/DtcSeverity";
import { FaultData } from "./Engine/FaultData";
import { Diagnostic } from "./Engine/Diagnostic";
import { FaultRichDataErrorType } from "./DataEnrichment/FaultRichDataErrorType";
import { FaultRichDataVendorSource } from "./DataEnrichment/FaultRichDataVendorSource";

/** A class that holds enrichment data for a fault. */
export interface FaultRichData extends EntityWithVersion {
  /** Gets or sets the vendor JSON data. */
  additionalData: string;
  /** Gets or sets the datetime. */
  dateTime: Date;
  /** Gets or sets the diagnostic */
  diagnostic: Diagnostic;
  /** Gets or sets the effect on component. */
  effectOnComponent: string;
  /** Gets or sets the engine hours . */
  engineHours: number;
  /** Gets or sets the Error Type. */
  errorType: FaultRichDataErrorType;
  /** Gets or sets the fault. */
  faultData: FaultData;
  /** Gets or sets the fault description. */
  faultDescription: string;
  /** Gets or sets the odometer. */
  odometer: number;
  /** Gets or sets the recommendation. */
  recommendation: string;
  /** Gets or sets the severity. */
  severity: DtcSeverity;
  /** Gets or sets the vendor source. */
  vendorSource: FaultRichDataVendorSource;
}
