//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "./Search";
import { DeviceSearch } from "./DeviceSearch";
import { TrailerSearch } from "./TrailerSearch";

/**
 *  The object used to specify the arguments when searching for {@link TrailerAttachment} record(s).
 */
export interface TrailerAttachmentSearch extends Search {
  /**
   * Gets or sets search for TrailerAttachments that were active at this date or after. Set to UTC now to search for
   *  only currently active TrailerAttachments.
   */
  activeFrom: Date;
  /** Gets or sets search for TrailerAttachments that were active at this date or before. */
  activeTo: Date;
  /**
   * Gets or sets search for TrailerAttachments attached to this {@link DeviceSearch} Id. Available DeviceSearch options are:.
   *  <list><item><description>Id</description></item></list>
   */
  deviceSearch: DeviceSearch;
  /** Gets or sets the maximum row version of the TrailerAttachment search criteria. */
  maxVersion: number;
  /**
   * Gets or sets search for TrailerAttachments with this {@link TrailerSearch} Id. Available TrailerSearch options are:.
   *  <list><item><description>Id</description></item></list>
   */
  trailerSearch: TrailerSearch;
  /** Gets or sets the row version of the TrailerAttachment search criteria. */
  version: number;
}
