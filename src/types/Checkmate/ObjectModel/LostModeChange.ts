//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { EntityWithVersion } from "./EntityWithVersion";
import { Device } from "./Device";

/** Represents a {@link LostModeChange} event for a device, tracking when Lost Mode was activated and deactivated. */
export interface LostModeChange extends EntityWithVersion {
  /** Gets or sets the {@link DateTime} when Lost Mode was activated. */
  activeFrom: Date;
  /** Gets or sets the {@link DateTime} when Lost Mode was deactivated. */
  activeTo: Date;
  /** Gets or sets the {@link LostModeChange.Device} associated with this Lost Mode change. */
  device: Device;
  /** Gets or sets the reason why Lost Mode was ended. */
  endReason: string;
}
