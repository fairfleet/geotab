//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { NameEntityWithVersion } from "../NameEntityWithVersion";
import { MaintenanceSchedule } from "../MaintenanceSchedule";
import { EventType } from "../EventType";
import { MaintenanceWorkOrderJob } from "./MaintenanceWorkOrderJob";
import { Device } from "../Device";

/** Maintenance Work Request for a particular device. */
export interface MaintenanceWorkRequest extends NameEntityWithVersion {
  /** Gets or sets the currency cost for the work request. */
  costCurrency: string;
  /** Gets or sets the current engine hours of the {@link MaintenanceWorkRequest.Device}. */
  currentEngineHours: string;
  /** Gets the current engine hours of the {@link MaintenanceWorkRequest.Device} rounded to the nearest whole hour. */
  currentEngineHoursInHours: number;
  /** Gets or sets the current odometer of the {@link MaintenanceWorkRequest.Device}. */
  currentOdometer: number;
  /** Gets or sets the current odometer of the {@link MaintenanceWorkRequest.Device} but in user preferred unit. */
  currentOdometerDisplay: number;
  /** Gets or sets the date the work request was created. */
  dateTime: Date;
  /**
   * @inheritdoc
   */
  deletedDateTime: Date;
  /** Gets or sets the description of the work request. */
  description: string;
  /** Gets or sets the {@link MaintenanceWorkRequest.Device} related to the work request. */
  device: Device;
  /** Gets or sets the date the work request is due. */
  dueOnDate: Date;
  /** Gets or sets the estimate cost for the work request. */
  estimateCost: number;
  /** Gets or sets the datetime of when the work request should expire. */
  expirationDate: Date;
  /** Gets or sets the impact of the work request. */
  impact: string;
  /** Gets or sets whether the work request is overdue or not. */
  isOverdue: boolean;
  /** Gets or sets whether the work request is pending or not to be solved. */
  isPending: boolean;
  /** Gets or sets whether the work request is visible or not. */
  isVisible: boolean;
  /** Gets or sets the labels. */
  labels: string[];
  /** Gets or sets the last completed work order job related to this work request. */
  lastCompletedWorkOrderJob: MaintenanceWorkOrderJob;
  /** Gets or sets the engine hours from the last maintenance done. Only used if work request is scheduled. */
  lastEngineHours: string;
  /** Gets the engine hours in hours from the last maintenance done. Only used if work request is scheduled. */
  lastEngineHoursInHours: number;
  /** Gets or sets the datetime of the last maintenance performed. This can be populated even if the work request is not scheduled, but it is only relevant and used when the work request is scheduled. */
  lastMaintenanceDate: Date;
  /** Gets or sets the odometer of the last maintenance performed for the {@link MaintenanceWorkRequest.Device}. This can be populated even if the work request is not scheduled, but it is only relevant and used when the work request is scheduled. */
  lastOdometer: number;
  /** Gets or sets the engine hours of the last maintenance performed for the {@link MaintenanceWorkRequest.Device}. This can be populated even if the work request is not scheduled, but it is only relevant and used when the work request is scheduled. */
  lastOdometerDisplay: number;
  /** Gets or sets the maintenance {@link EventType} of the work request. */
  maintenanceType: EventType;
  /** Gets or sets the metadata of the work request. */
  metadata: { [key: string]: string };
  /** Gets or sets the recommendation for the work request. */
  recommendation: string;
  /** Gets a value indicating whether determines if the work request requires calculation when new (only for scheduled work requests). */
  requiresCalculation: boolean;
  /** Gets or sets the {@link MaintenanceSchedule} related to the work request. */
  schedule: MaintenanceSchedule;
  /** Gets or sets the remaining engine hours of the {@link MaintenanceWorkRequest.Device} before the work request becomes overdue. A negative value indicates that the service is overdue. */
  serviceDueInEngineHours: string;
  /** Gets the remaining engine hours of the {@link MaintenanceWorkRequest.Device} before the work request becomes overdue rounded to the nearest whole hour. A negative value indicates that the service is overdue . */
  serviceDueInEngineHoursInHours: number;
  /** Gets or sets the remaining distance of the {@link MaintenanceWorkRequest.Device} before the work request becomes overdue. */
  serviceDueInKilometers: number;
  /** Gets or sets the odometer reading in miles of the {@link MaintenanceWorkRequest.Device} at which the work request is due. A negative value indicates that the service is overdue. */
  serviceDueInMiles: number;
  /** Gets or sets the engine hours of the {@link MaintenanceWorkRequest.Device} at which the work request is due. */
  serviceDueOnEngineHours: string;
  /** Gets the engine hours of the {@link MaintenanceWorkRequest.Device} at which the work request is due rounded to the nearest whole hour. */
  serviceDueOnEngineHoursInHours: number;
  /** Gets or sets the odometer reading the {@link MaintenanceWorkRequest.Device} at which the work request is due. */
  serviceDueOnOdometer: number;
  /** Gets or sets the severity of the work request. */
  severity: number;
  /** Gets or sets the severity of the work request. */
  severityDisplay: number;
  /** Gets or sets the date when the work request was snoozed. */
  snoozeDate: Date;
  /** Gets or sets the date until which the work request is snoozed. */
  snoozeUntilDate: Date;
  /** Gets or sets the triggered dateTime of the work request. */
  triggeredDateTime: Date;
  /** Gets or sets the date the work request was updated. */
  updateDate: Date;
}
