//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "../Search";
import { DeviceSearch } from "../DeviceSearch";
import { MaintenanceSeverity } from "./MaintenanceSeverity";
import { MaintenanceScheduleSearch } from "../MaintenanceScheduleSearch";
import { EventTypeSearch } from "../EventTypeSearch";
import { DueCriteriaSearch } from "./DueCriteriaSearch";

/** The object used to specify the arguments when searching for {@link MaintenanceWorkRequest}. */
export interface MaintenanceWorkRequestSearch extends Search {
  /**
   * Gets or sets search for {@link MaintenanceWorkRequest}s matching this
   *  {@link DeviceSearch}. Providing a device ID will
   *  search for any MaintenanceWorkRequest that are assigned to that Device.
   *  Providing the Groups will search for MaintenanceWorkRequest for that have Devices in that group.
   *  Available DeviceSearch options are:.
   *  <list><item><description>Id</description></item><item><description>DeviceIds</description></item><item><description>Groups</description></item><item><description>GroupFilterCondition</description></item></list>
   */
  deviceSearch: DeviceSearch;
  /** Gets or sets search for Work Requests based on due-related parameters. */
  dueCriteriaSearch: DueCriteriaSearch;
  /** Gets or sets search for {@link MaintenanceWorkRequest}s updated at this date or after. */
  fromDate: Date;
  /** Gets or sets a value indicating whether search includes records that have the dtDeleted column set */
  includeDeleted: boolean;
  /** Gets or sets whether the {@link MaintenanceWorkRequest}s are overdue or not. */
  isOverdue: boolean;
  /** Gets or sets whether the {@link MaintenanceWorkRequest}s are reviewed or not. */
  isReviewed: boolean;
  /** Gets or sets whether the {@link MaintenanceWorkRequest}s are snoozed or not. */
  isSnoozed: boolean;
  /** Gets or sets whether the {@link MaintenanceWorkRequest}s are visible or not. */
  isVisible: boolean;
  /**
   * Gets or sets search for Work Requests matching this
   *  {@link EventTypeSearch}.
   *  Available EventTypeSearch options are:.
   *  <list><item><description>Ids</description></item><item><description>Labels</description></item></list>
   */
  maintenanceTypeSearch: EventTypeSearch;
  /**
   * Gets or sets search for {@link MaintenanceWorkRequest}s matching this
   *  {@link MaintenanceScheduleSearch}. Providing a schedule id will
   *  search for any MaintenanceWorkRequest that are assigned to that Schedule.
   *  Available ScheduleSearch options are:
   *  <list><item><description>Id</description></item><item><description>ScheduleIds</description></item></list>
   */
  scheduleSearch: MaintenanceScheduleSearch;
  /** Gets or sets the list of severities to search by. */
  severities: MaintenanceSeverity[];
  /** Gets or sets the list of sources to search by. */
  sources: string[];
}
