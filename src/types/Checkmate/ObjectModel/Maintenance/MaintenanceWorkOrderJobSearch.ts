//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "../Search";
import { DeviceSearch } from "../DeviceSearch";
import { MaintenanceScheduleSearch } from "../MaintenanceScheduleSearch";
import { EventTypeSearch } from "../EventTypeSearch";
import { ReportPeriod } from "../ReportPeriod";

/** The object used to specify the arguments when searching for {@link MaintenanceWorkOrderJob}. */
export interface MaintenanceWorkOrderJobSearch extends Search {
  /** Gets or sets the period for completed date filtering. */
  completedDatePeriod: ReportPeriod;
  /**
   * Gets or sets search for {@link MaintenanceWorkOrderJob}s matching this
   *  {@link DeviceSearch}.
   *  Providing a device ID will search for any MaintenanceWorkOrderJob that are assigned to that Device.
   *  Providing the DeviceIds will search for MaintenanceWorkOrderJob that are assigned to those Devices.
   *  Providing the Groups will search for MaintenanceWorkOrderJob for that have Devices in that group.
   *  Available DeviceSearch options are:.
   *  <list><item><description>Id</description></item><item><description>DeviceIds</description></item><item><description>Groups</description></item></list>
   */
  deviceSearch: DeviceSearch;
  /** Gets or sets search for {@link MaintenanceWorkOrderJob}s updated at this date or after. */
  fromDate: Date;
  /** Gets or sets search for {@link MaintenanceWorkOrderJob}s completed at this date or after. */
  fromDateCompleted: Date;
  /** Gets or sets search for {@link MaintenanceWorkOrderJob}s started at this date or after. */
  fromDateStarted: Date;
  /** Gets or sets a value indicating whether search includes records that have the dtDeleted column set */
  includeDeleted: boolean;
  /**
   * Gets or sets search for Work Jobs matching this
   *  {@link EventTypeSearch}.
   *  Available EventTypeSearch options are:.
   *  <list><item><description>Ids</description></item><item><description>Labels</description></item><item><description>DefectSearch</description></item></list>
   */
  maintenanceTypeSearch: EventTypeSearch;
  /**
   * Gets or sets search for {@link MaintenanceWorkOrderJob}s matching this
   *  {@link MaintenanceScheduleSearch}. Providing a schedule id will
   *  search for any MaintenanceWorkOrderJob that are assigned to that Schedule.
   *  Available ScheduleSearch options are:
   *  <list><item><description>Id</description></item><item><description>ScheduleIds</description></item></list>
   */
  scheduleSearch: MaintenanceScheduleSearch;
  /** Gets or sets the list of sources to search by. */
  sources: string[];
  /** Gets or sets the period for started date filtering. */
  startedDatePeriod: ReportPeriod;
  /** Gets or sets the list of statuses to search by. */
  statuses: number[];
  /** Gets or sets search for {@link MaintenanceWorkOrderJob}s completed at this date or before. */
  toDateCompleted: Date;
  /** Gets or sets search for {@link MaintenanceWorkOrderJob}s started at this date or before. */
  toDateStarted: Date;
  /** Gets or sets search for {@link MaintenanceWorkOrderJob}s that belong or not to an order. */
  withWorkOrder: boolean;
  /** Gets or sets search for {@link MaintenanceWorkOrderJob}s linked to the {@link MaintenanceWorkOrder}. */
  workOrderId: string;
}
