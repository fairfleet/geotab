//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { NameEntity } from "../NameEntity";
import { Diagnostic } from "./Diagnostic";

/**
 * Represents a "virtual diagnostic" which is configurable per device as a {@link DiagnosticMapping} and
 *  must be resolved along with a {@link Device} to get the actual {@link Diagnostic}.
 */
export interface MappedDiagnostic extends NameEntity {
  /**
   * Gets or sets the default diagnostic that must be used when a {@link DiagnosticMapping} does not exist
   *  for a {@link Device} and this {@link MappedDiagnostic}.
   */
  defaultDiagnostic: Diagnostic;
}
