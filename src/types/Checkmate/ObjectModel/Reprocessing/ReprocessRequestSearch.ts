//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "../Search";
import { ReprocessRequestStatus } from "./ReprocessRequestStatus";

/** The object used to specify the arguments when searching for a {@link ReprocessRequest}. */
export interface ReprocessRequestSearch extends Search {
  /** Gets or sets the FromDate of the {@link ReprocessRequest} to search by. */
  fromDate: Date;
  /** Gets a value indicating whether any search criteria have been specified. */
  isEmpty: boolean;
  /** Gets or sets the {@link ReprocessRequestStatus} to search by. */
  status: ReprocessRequestStatus;
  /** Gets or sets the ToDate of the {@link ReprocessRequest} to search by. */
  toDate: Date;
}
