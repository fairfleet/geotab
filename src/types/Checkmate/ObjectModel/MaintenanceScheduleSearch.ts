//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "./Search";
import { EventTypeSearch } from "./EventTypeSearch";
import { GroupSearch } from "./GroupSearch";
import { GroupFilterCondition } from "./GroupFilterCondition";
import { IncludeGroups } from "./IncludeGroups";
import { DeviceSearch } from "./DeviceSearch";

/**
 *  The object used to specify the
 *  arguments when searching for {@link MaintenanceSchedule}(s).
 *
 * @deprecated
 */
export interface MaintenanceScheduleSearch extends Search {
  /** Gets or sets search for all Active {@link MaintenanceSchedule}. */
  active: boolean;
  /** Gets or sets a value indicating whether to filter for EventRules that have the days recurrence field populated. */
  days: boolean;
  /**
   * Gets or sets search for {@link MaintenanceSchedule}s matching this
   *  {@link MaintenanceScheduleSearch.DeviceSearch}. Used to filter devices when calculating work request counts.
   *  Available DeviceSearch options are:.
   *  <list><item><description>FromDate</description></item></list>
   */
  deviceSearch: DeviceSearch;
  /** Gets or sets a value indicating whether to filter for EventRules that are set to not generate recurring events. */
  engineHours: boolean;
  /** Gets or sets a value indicating whether to filter for EventRules by the event type id. */
  eventTypeId: string;
  /** Gets or sets a value indicating a list of {@link EventType} Id-s to search by. */
  eventTypeIds: string[];
  /** Gets or sets the {@link MaintenanceScheduleSearch.EventTypeSearch} to search for {@link MaintenanceSchedule} by. */
  eventTypeSearch: EventTypeSearch;
  /** Gets or sets the groups to find the event rules belonging to parents or children of the groups using and/or relationship. */
  groupFilterCondition: GroupFilterCondition;
  /**
   * Gets or sets search for Event Rules that are members of these {@link GroupSearch}(s) one of
   *  it's children or one of it's parents.
   *  Available GroupSearch options are:.
   *  <list><item><description>Id</description></item></list>
   */
  groups: GroupSearch[];
  /** Gets or sets a value indicating whether search includes records that have the dtDeleted column set */
  includeDeleted: boolean;
  /** Gets or sets a value indicating how to filter on Group. */
  includeGroups: IncludeGroups;
  /** Gets or sets a value indicating whether to include vehicle work request count for the returned schedules. */
  includeWorkRequestCount: boolean;
  /** Gets or sets a value indicating whether to filter for EventRules that have the kilometers recurrence field populated. */
  kilometers: boolean;
  /** Gets or sets a value indicating whether to filter for EventRules that have the months recurrence field populated. */
  months: boolean;
  /** Gets or sets search for all {@link MaintenanceSchedule} matching this Name. Wildcard can be used by prepending/appending "%" to string. Example "%name%". */
  name: string;
  /** Gets or sets search for all {@link MaintenanceSchedule} matching this Name or in their labelds. Wildcard can be used by prepending/appending "%" to string. Example "%name%". */
  nameOrLabels: string;
  /** Gets or sets a value indicating whether to filter for EventRules that have the datetime one-off field populated. */
  nonRepeating: boolean;
  /** Gets or sets a value indicating whether to filter for EventRules that are set to generate recurring events. */
  repeats: boolean;
  /**
   * Gets or sets search for Schedules with these unique {@link Id}(s). Not Supported
   *  for searching for schedules, only for {@link MaintenanceWorkRequest}.
   */
  scheduleIds: string[];
  /** Gets or sets search for all {@link MaintenanceSchedule} matching this Source. */
  source: string;
  /** Gets or sets a value indicating whether to filter for EventRules that have the weeks recurrence field populated. */
  weeks: boolean;
}
