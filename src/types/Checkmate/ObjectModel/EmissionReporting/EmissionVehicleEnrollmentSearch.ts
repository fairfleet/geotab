//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "../Search";
import { DeviceSearch } from "../DeviceSearch";

/** Search class for {@link EmissionVehicleEnrollment}. */
export interface EmissionVehicleEnrollmentSearch extends Search {
  /**
   * Gets or sets {@link EmissionVehicleEnrollmentSearch.DeviceSearch} to filter {@link EmissionVehicleEnrollmentSearch} result.
   *  Available {@link EmissionVehicleEnrollmentSearch.DeviceSearch} options are:
   *  <list><item><description>Id</description></item><item><description>DeviceIds</description></item></list>
   */
  deviceSearch: DeviceSearch;
  /** Gets or sets a list of enrollment {@link Id}(s) to search for {@link EmissionVehicleEnrollment}s matching one of the enrollment {@link Id}s. */
  enrollmentIds: string[];
  /**
   * Gets or sets a list of enrollment statuses to search for {@link EmissionVehicleEnrollment}s matching one of the statuses.
   *  Valid "EnrollmentStatus" values are:
   *  <list><item><description>NotEnrolled</description></item><item><description>Pending</description></item><item><description>Enrolled</description></item><item><description>Rejected</description></item></list>
   */
  enrollmentStatuses: string[];
  /** Gets or sets a value to search for all {@link EmissionVehicleEnrollment}s from this date. */
  fromDate: Date;
  /** Gets or sets search for all {@link EmissionVehicleEnrollment}s with DMV registration {@link DateTime} from this date. */
  fromDmvRegistrationDateTime: Date;
  /**
   * Gets or sets a value indicating whether previously disenrolled {@link EmissionVehicleEnrollment}s are included.
   *  If true, returns active enrollments and disenrolled enrollments;
   *  Otherwise, only returns current active enrollments.
   *  (An active enrollment has enrollment status Pending or Enrolled.)
   *  Default is false.
   */
  includeDisenrolledEnrollments: boolean;
  /**
   * Gets or sets a value indicating whether rejected {@link EmissionVehicleEnrollment}s are included.
   *  If true, returns all active enrollments and rejected enrollments;
   *  Otherwise, only returns active enrollments.
   *  (An active enrollment has enrollment status Pending or Enrolled.)
   *  Default is false.
   */
  includeRejectedEnrollments: boolean;
  /** Gets or sets search for all {@link EmissionVehicleEnrollment}s with the given {@link EmissionEnrollmentLoggingFrequencyType} value. */
  loggingFrequencyType: number;
  /** Gets or sets the maximum version of the {@link EmissionVehicleEnrollmentSearch} search criteria. */
  maxVersion: number;
  /** Gets or sets a value to search for all {@link EmissionVehicleEnrollment}s to this date. */
  toDate: Date;
  /** Gets or sets search for all {@link EmissionVehicleEnrollment}s with DMV registration {@link DateTime} to this date. */
  toDmvRegistrationDateTime: Date;
  /** Gets or sets the version of the {@link EmissionVehicleEnrollmentSearch} search criteria. */
  version: number;
}
