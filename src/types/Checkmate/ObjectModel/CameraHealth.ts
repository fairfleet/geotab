//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { EntityWithVersion } from "./EntityWithVersion";
import { Camera } from "./Camera";
import { CameraHealthStatus } from "./CameraHealthStatus";

/** Stores the camera health status information. */
export interface CameraHealth extends EntityWithVersion {
  /** Gets or sets the camera. */
  camera: Camera;
  /** Gets or sets the timestamp when the health status was last updated. */
  lastUpdated: Date;
  /** Gets or sets the camera health status. */
  status: CameraHealthStatus;
}
