//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { Search } from "../Search";
import { UserSearch } from "../UserSearch";
import { PushNotificationPlatform } from "./PushNotificationPlatform";
import { PushNotificationService } from "./PushNotificationService";

/** The object used to specify the arguments when searching for {@link PushToken}s. */
export interface PushTokenSearch extends Search {
  /** Gets or sets the ID of the app the token belongs to. E.g. com.geotab.mygeotab for the MyGeotab app. */
  appId: string;
  /** Gets or sets search for push tokens that were updated at this date or after. */
  fromDateTimeUpdated: Date;
  /** Gets or sets the {@link PushNotificationPlatform} the token is sourced from. */
  platform: PushNotificationPlatform;
  /** Gets or sets the {@link PushNotificationService} the token is associated with. */
  service: PushNotificationService;
  /** Gets or sets search for push tokens that were updated at this date or before. */
  toDateTimeUpdated: Date;
  /** Gets or sets the value of the notification service's token. */
  token: string;
  /**
   * Gets or sets search for PushTokens with this {@link UserSearch} Id.
   *  Available UserSearch options are:.
   *  <list><item><description>Id</description></item></list>
   */
  userSearch: UserSearch;
}
