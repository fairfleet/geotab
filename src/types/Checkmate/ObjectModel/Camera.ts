//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

import { EntityWithVersion } from "./EntityWithVersion";
import { CameraPartner } from "./CameraPartner";

/** Stores mapping between partner cameras and Geotab devices. */
export interface Camera extends EntityWithVersion {
  /** Gets or sets the active from timestamp. */
  activeFrom: Date;
  /** Gets or sets the active to timestamp. */
  activeTo: Date;
  /** Gets or sets the device serial number. */
  deviceSerialNumber: string;
  /** Gets or sets the camera model. */
  model: string;
  /** Gets or sets the partner device id. */
  partnerDeviceId: string;
  /** Gets or sets the {@link CameraPartner}. */
  partnerId: CameraPartner;
}
